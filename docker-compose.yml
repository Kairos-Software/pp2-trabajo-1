services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: questions_db
    ports:
      - "5433:5432"   # <--- solo cambiá este número
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
